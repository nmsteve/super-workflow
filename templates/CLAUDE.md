# Active Working Rules

This file mirrors the reusable workflow guidance from `AGENTS.md` for Claude Code.

@AGENTS.md

## Claude Code Notes

Claude Code reads `CLAUDE.md` for project and user memory. Keep shared workflow rules in `AGENTS.md` when possible and import them here, or keep both files synchronized if your environment does not use imports.
